# Chrome H.264 VAAPI Encoder Hotpatch Distribution

This folder contains the redistributable package created from the experiment.

Use:

```bash
./chrome-vaapi-h264-hotpatch/run-chrome-h264-vaapi.sh
```

Package archive:

```text
chrome-vaapi-h264-hotpatch.tar.gz
```
